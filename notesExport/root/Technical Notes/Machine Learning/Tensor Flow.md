# Tensor Flow
*   tensdor flow open serouc libray in c++ with python pindings and othe rlanguage bindings
*   relatively low level ml lib fo creating ml  stuff
*   what is a tensor, an n dimensional matrix
*   deeplearn.js → tensor flow.js
    *   webgl implementaiotn

note this somwerhe else

ml libraries

from scracth

then tensorflow → keras

tensorflow js (but has keras stuff???)→ ml5