# Commands
Commands
--------

These are what you typically think bots do. Listen for a particular command and fire of an appropriate function

The ‘cheap’ command
-------------------

The easiest way is to parse every message sent to the server and fire off an appropriate command.

```text-plain
 asdf
```

Registering an actual command
-----------------------------

use this file

### Deleting commands 

just ignore and delete all and then upload all again if you are running this

Listening to a command
----------------------

*   It's all a matter of matching the command name;

Handling n number of commands
-----------------------------

misc notes
----------

*   body should be array of jsons
*   the command options callback must return the options

* * *

*   registering a command
    *   required and drescription
    *   removing a command
    *   just do it on every server startup
*   listening to a command
*   creating a choices
*   see more for buttons and menus